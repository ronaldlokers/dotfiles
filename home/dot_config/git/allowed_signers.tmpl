{{- /* Who git trusts to have signed a commit. Same source as
       user.signingkey, so a rotation is one edit in the vault. */ -}}
{{- $key := includeTemplate "signing-pubkey" . }}
{{- if $key }}
{{ .email }} {{ $key }}
{{- end }}
