{{- /* "true" or "false": are we inside a container? /.dockerenv is docker's
       marker, /run/.containerenv podman's. Shared by every host-only gate. */ -}}
{{- if or (stat "/.dockerenv") (stat "/run/.containerenv") -}}true{{- else -}}false{{- end -}}
