{{- /* "true" or "false": does the devpod external ship a binary for this
       platform? linux-amd64 only. Shared by the external and the configure
       script so an unsupported platform is distinguishable from a missing
       binary. */ -}}
{{- if and (eq .chezmoi.os "linux") (eq .chezmoi.arch "amd64") -}}true{{- else -}}false{{- end -}}
