.config/environment.d/10-ssh-agent.conf
# Config directories left behind by tools dropped from
# dot_config/mise/config.toml. ~/.local/bin/gemini is deliberately not listed:
# it is one of a family of hand-written wrapper scripts (codex, copilot,
# opencode, pi, ...) this repo neither manages nor created.
.config/superfile
.local/share/superfile
.gemini
